# =============================================================================
# BAUER GROUP XPD-RPIImage - Semantic Release Workflow
# =============================================================================
# Triggers:
#   - Push to main branch (excluding docs/markdown and workflow changes)
#   - Manual dispatch with force-release option
#
# Responsibility:
#   * bump `.variant.version` in config/variants/base.json
#   * write CHANGELOG.md
#   * create tag `vX.Y.Z` and a GitHub release with auto-generated notes
#
# Image artifacts (.img.xz + .sha256 + .manifest.json) are attached to the
# release by build.yml's `release` job. NOTE: the tag is pushed here with
# GITHUB_TOKEN, and GitHub does not trigger other workflows from
# GITHUB_TOKEN events, so build.yml does NOT fire automatically on the tag.
# Dispatch it by hand to populate the assets:
#   gh workflow run build.yml --ref v<version>
# (see docs/ci-cd.md -> "When it triggers" for the full rationale)
# =============================================================================

name: 🚀 Release

on:
  push:
    branches: [main]
    paths-ignore:
      - '.github/**'
      - '*.md'
      - 'docs/**'
      - 'LICENSE'
      - '.gitignore'
      # Pages site + its renderer are cosmetic; never cut a release for them.
      - 'site/**'
      - 'scripts/build_pages.py'
  workflow_dispatch:
    inputs:
      force-release:
        description: 'force create release'
        type: boolean
        default: false

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  # ============================================
  # Release Job
  # ============================================

  release:
    name: 📦 Create Semantic Release
    uses: bauer-group/automation-templates/.github/workflows/modules-semantic-release.yml@main
    with:
      target-branch: 'main'
      dry-run: false
      force-release: ${{ inputs.force-release || false }}
      extra-plugins: '@google/semantic-release-replace-plugin'
    secrets: inherit
