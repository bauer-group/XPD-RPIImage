# =============================================================================
# BAUER GROUP XPD-RPIImage - environment variable template
#
# Copy this file to .env and fill in real values. The generator resolves
# ${VAR} / ${VAR:-default} references inside the variant JSON at build time.
# Unresolved required variables (no default) fail the build fast.
#
# .env is gitignored - do NOT commit it.
# =============================================================================

# --- credentials baked into the image ------------------------------------
# Admin user password set via chpasswd in the bgrpiimage-users module.
# Referenced by every variant's users[].password.
ADMIN_PASSWORD=12345678

# Pre-shared key for a WiFi network baked in at build time.
# ONLY relevant if you set network.wifi.mode to dhcp/static AND add entries to
# network.wifi.networks[] in your variant. The shipped variants have WiFi
# disabled and reference no PSK, so leaving this unset changes nothing.
# WIFI_PSK=

# --- optional: override the CustomPiOS toolchain (docker image + git ref) -
# Both are already pinned in-repo: CUSTOMPIOS_REF to a full commit SHA in
# scripts/bootstrap.sh (CI overrides it in .github/workflows/build.yml), and
# DOCKER_IMAGE to a commit-scoped ghcr.io tag in scripts/build.sh. Set these
# only to test a different upstream revision - use a full 40-char SHA, not a
# branch or tag, so the pin cannot drift under you.
# DOCKER_IMAGE=ghcr.io/guysoft/custompios:sha-d293309
# CUSTOMPIOS_REF=d293309aac2f606c609645b441962c8f02b6e8c3

# --- optional: tools container image name --------------------------------
# BGRPIIMAGE_TOOLS_IMAGE=bgrpiimage-tools
