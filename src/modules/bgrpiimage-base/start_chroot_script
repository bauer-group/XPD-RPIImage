#!/usr/bin/env bash
# bgrpiimage-base: build-time adapter. Logic lives in apply.sh.
set -x
set -e
export LC_ALL=C
export DEBIAN_FRONTEND=noninteractive

source /common.sh
install_cleanup_trap

unpack /filesystem/root / root

BGRPI_CTX=image /opt/bgrpiimage/bgrpiimage-base/apply.sh
